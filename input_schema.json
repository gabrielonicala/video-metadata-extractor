{
  "title": "Video Metadata Extractor",
  "description": "Extract metadata, comments, and download videos from YouTube, TikTok, Twitter/X, and Instagram",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "Video URL",
      "type": "string",
      "description": "YouTube, TikTok, Twitter/X, or Instagram video URL",
      "editor": "textfield",
      "nullable": false
    },
    "extractComments": {
      "title": "Extract Comments",
      "type": "boolean",
      "description": "Also extract video comments (may take longer)",
      "default": false,
      "editor": "checkbox"
    },
    "useProxy": {
      "title": "Use Apify Residential Proxy",
      "type": "boolean",
      "description": "Use Apify's built-in residential proxy for reliable extraction (recommended)",
      "default": true,
      "editor": "checkbox"
    },
    "maxComments": {
      "title": "Max Comments",
      "type": "integer",
      "description": "Maximum number of comments to extract (if extractComments is enabled)",
      "default": 50,
      "minimum": 1,
      "maximum": 500,
      "editor": "number"
    },
    "downloadVideo": {
      "title": "Download Video",
      "type": "boolean",
      "description": "Download the video file to Apify storage (consumes proxy bandwidth)",
      "default": false,
      "editor": "checkbox"
    }
  },
  "required": ["url"]
}
