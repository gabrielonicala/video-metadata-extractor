{
  "title": "Video Metadata Extractor",
  "description": "Extract metadata, comments, and direct video URLs from YouTube, TikTok, and Twitter/X. Automatically uses Apify's residential proxy when needed (TikTok, Instagram).",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "Video URL",
      "type": "string",
      "description": "YouTube, TikTok, or Twitter/X video URL",
      "editor": "textfield",
      "nullable": false
    },
    "extractComments": {
      "title": "Extract Comments",
      "type": "boolean",
      "description": "Also extract video comments (may take longer)",
      "default": false,
      "editor": "checkbox"
    },
    "maxComments": {
      "title": "Max Comments",
      "type": "integer",
      "description": "Maximum number of comments to extract (if extractComments is enabled)",
      "default": 50,
      "minimum": 1,
      "maximum": 500,
      "editor": "number"
    },
    "downloadVideo": {
      "title": "Download Video",
      "type": "boolean",
      "description": "Include a video URL. For YouTube returns a direct link; for TikTok/Twitter downloads and stores in Apify's key-value store.",
      "default": false,
      "editor": "checkbox"
    },
    "videoQuality": {
      "title": "Video Quality",
      "type": "string",
      "description": "Preferred video quality (default: best available)",
      "default": "best",
      "enum": ["best", "1080p", "720p", "480p", "360p"],
      "editor": "select"
    },
    "twitterCookiesFile": {
      "title": "Twitter/X Cookies File",
      "type": "string",
      "description": "Path to a Netscape-format cookies file for Twitter/X (e.g. twitter_cookies.txt). Required to extract replies — export via the 'Get cookies.txt LOCALLY' browser extension after logging into x.com. Leave blank to attempt without auth (most replies will be hidden).",
      "default": "twitter_cookies.txt",
      "editor": "textfield"
    }
  },
  "required": ["url"]
}
