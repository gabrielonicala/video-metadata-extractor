{
  "title": "Video Metadata Extractor",
  "description": "Extract metadata, comments, and direct video URLs from YouTube, TikTok, and Twitter/X. Automatically uses Apify's residential proxy when needed (TikTok, Instagram).",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "Video URL",
      "type": "string",
      "description": "YouTube, TikTok, or Twitter/X video URL",
      "editor": "textfield",
      "nullable": false
    },
    "extractComments": {
      "title": "Extract Comments",
      "type": "boolean",
      "description": "Also extract video comments (may take longer)",
      "default": false,
      "editor": "checkbox"
    },
    "maxComments": {
      "title": "Max Comments",
      "type": "integer",
      "description": "Maximum number of comments to extract (if extractComments is enabled)",
      "default": 50,
      "minimum": 1,
      "maximum": 500,
      "editor": "number"
    },
    "downloadVideo": {
      "title": "Get Video Download URL",
      "type": "boolean",
      "description": "Extract direct video URL (signed, expires after ~6 hours). Uses residential proxy for TikTok/Instagram automatically.",
      "default": false,
      "editor": "checkbox"
    }
  },
  "required": ["url"]
}
